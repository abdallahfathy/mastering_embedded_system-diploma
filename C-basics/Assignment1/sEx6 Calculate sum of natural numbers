#include"stdio.h"

void main (){
	int num,i,sum=0;
	printf("Enter an Integer : ");
	fflush (stdin);fflush (stdout);
	scanf ("%d" ,&num);
	for(i=0;i<num;i++){
		sum=sum+i;
	}
	printf("sum = %d",sum);
}

