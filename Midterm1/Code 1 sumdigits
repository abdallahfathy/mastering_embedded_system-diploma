#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
void sumdig (void);

int main(void)
{

	sumdig();
	return 0;
}
void sumdig (void){
	int num,rem=0;
	scanf("%d",&num);
	fflush(stdin);
	while(num>0)
	{
		rem=rem+num%10;
		num=num/10;
	}
printf("%d",rem);

}
