#include <stdio.h>
#include <stdlib.h>
#define pi 3.14
#define radius printf("Enter the radius : ")
#define area(r) (pi*r*r)


int main()
{
	float area,x;
	radius ;
	fflush(stdin);fflush(stdout);
	scanf("%f",&x);
	area=area(x);
	printf("The area = %.2f",area);
	return 0;
}
